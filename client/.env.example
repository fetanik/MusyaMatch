# API origin only (no trailing /api). Example: http://localhost:5000
# If you use http://localhost:5000/api here, paths are still resolved correctly.
VITE_API_BASE_URL=http://localhost:5000

# Copy to client/.env and set your key (do not commit client/.env). Maps JavaScript API + Places.
# VITE_GOOGLE_MAPS_API_KEY=
